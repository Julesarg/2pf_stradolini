import { FontsDirective } from './fonts.directive';

describe('FontsDirective', () => {
  it('should create an instance', () => {
    const directive = new FontsDirective();
    expect(directive).toBeTruthy();
  });
});
